import 'package:flutter/material.dart';

class ProgramsScreenFailure extends StatelessWidget {
  const ProgramsScreenFailure({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Error'),
    );
  }
}
