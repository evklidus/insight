import 'package:flutter/material.dart';

class ProgramsScreenEmpty extends StatelessWidget {
  const ProgramsScreenEmpty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Empty'),
    );
  }
}
