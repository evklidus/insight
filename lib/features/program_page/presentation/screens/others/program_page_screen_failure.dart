import 'package:flutter/material.dart';

class ProgramPageScreenFailure extends StatelessWidget {
  const ProgramPageScreenFailure({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Error'),
    );
  }
}
